using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace lecture19
{
    // decleration of the delegate the method must be the same signature(return type & parameters)
    public delegate void GameStages();
}